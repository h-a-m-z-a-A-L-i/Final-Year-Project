%!TEX root = ../_fyp.tex
\chapter{Results}
\label{c-results}

This chapter reports empirical outcomes from the three benchmark suites defined in Section~\ref{sec:evaluation}. All runs used GLM~4.7 on frozen Kaggle notebook snapshots with live \ac{API} inference. Tables summarise mode-level behaviour; narrative interpretation follows in Chapter~\ref{chap:discussion}.

\section{Experimental Setup}

Each suite comprised four scenario tests. Ask benchmarks use \texttt{run\_ask\_tests.py}. Code benchmarks use \texttt{run\_code\_tests.py}. Agentic benchmarks use \texttt{run\_agent\_tests.py}. Primary notebooks were Pakistan housing (kernel~113620421) and testing-ol (kernel~112732919). Ask and Code modes produced streamed natural-language replies; Agentic mode exercised the mandatory two-phase protocol described in Section~\ref{sec:tool-registry}.

\section{Agentic Mode Results}

Agentic evaluation focused on \textbf{Test~1 (Complete ML Pipeline)} as the primary evidence for tool orchestration. The agent was prompted to create a full machine-learning workflow on the Pakistan housing snapshot.

In this run the model consumed \textbf{two \ac{API} calls}. Round~0 dispatched read-only notebook queries. Round~1 dispatched an implement batch of \textbf{37 tools}: twenty-four each of \texttt{insert\_cell}, \texttt{edit\_cell\_by\_index}, and \texttt{run\_cell}, plus two read operations. Host-side batch reordering preserved writes-before-runs ordering per Section~\ref{sec:tool-registry}. Total wall time was approximately 9.8\,s with zero repair rounds recorded in the harness trace.

Table~\ref{tab:agentic-primary} records the primary Agentic result. This outcome demonstrates that GLM~4.7 can plan a multi-step notebook workflow as a single parallel tool batch after a codebase inspection call---a pattern aligned with tool-learning literature \parencite{schick2023toolformer,qin2024toolllm} but constrained to two model turns rather than an open ReAct loop \parencite{yao2023react}.

\begin{table}[!ht]
\centering
\caption{Primary Agentic benchmark result (Test~1, dispatch-only evaluation).}
\label{tab:agentic-primary}
\small
\begin{tabular}{|l|r|}
\hline
\textbf{Metric} & \textbf{Value} \\ \hline
Outcome & Pass (success) \\
LLM calls & 2 \\
Tools dispatched (round~1 batch) & 37 \\
Insert / edit / run / read & 24 / 24 / 24 / 2 \\
Execution time (s) & 9.8 \\
Repair rounds & 0 \\ \hline
\end{tabular}
\end{table}

The current Agentic implementation does \emph{not} include a full ReAct observe--act loop with per-cell replanning \parencite{yao2023react,shinn2023reflexion}. Evaluation therefore measures dispatch correctness only; execution confirmation in the live browser is left to subsequent turns or future work (Section~\ref{sec:future-react}).

\section{Ask Mode Results}

Ask mode maintained \textbf{zero tool calls} and \textbf{zero notebook writes} across all runs, satisfying the read-only contract \parencite{vaithilingam2022expectation}. Table~\ref{tab:ask-results} summarises the three tests reported in this thesis.

\begin{table}[!ht]
\centering
\caption{Ask mode benchmark outcomes.}
\label{tab:ask-results}
\small
\begin{tabular}{|p{3.8cm}|c|p{5.8cm}|}
\hline
\textbf{Test} & \textbf{Result} & \textbf{Observation} \\ \hline
Explain notebook workflow & Strong & 89\% topic coverage; demonstrates context awareness of workflow stages \\
Explain cell~17 & Pass & Accurate cell explanation; 0\% heuristic hallucination \\
Debug error (cell~31) & Pass & Correct root-cause diagnosis; minimal fix suggested \\ \hline
\end{tabular}
\end{table}

\textbf{Limitation.} When packed context does not span every cell in a long notebook, workflow-level answers may remain high-level even when topic coverage is strong---a known constraint of context-grounded assistants \parencite{lewis2020rag,wei2022chain}. Cell-local tasks (Tests~2--3) remained reliable under the same snapshot conditions.

\section{Code Mode Results}

Code mode maintained \textbf{zero browser tool calls} and \textbf{zero notebook writes} on all runs. Table~\ref{tab:code-results} lists the four reported tests.

\begin{table}[!ht]
\centering
\caption{Code mode benchmark outcomes.}
\label{tab:code-results}
\small
\begin{tabular}{|p{3.6cm}|c|p{5.6cm}|}
\hline
\textbf{Test} & \textbf{Result} & \textbf{Observation} \\ \hline
XGBoost pipeline generation & Pass & Code correctness 100\%; valid training cell using notebook variables \\
Cell~21 replacement & Pass & Robust preprocessing replacement with placement guidance \\
Feature-engineering module & Pass & Interaction, polynomial, and missing-value handling \\
Empty cell workflow & Pass & Asked user intent; deferred full code per \texttt{CODE\_MODE.txt} \\ \hline
\end{tabular}
\end{table}

\textbf{Limitation.} Placement guidance can misalign with the true notebook execution flow when context depth is limited---for example, citing early cells rather than the model-training section---even when the generated code is syntactically and semantically correct \parencite{mozannar2024reading,mcnutt2023notebookassistants}.

\section{Cross-Mode Summary}

Table~\ref{tab:cross-mode} contrasts the three modes at a glance. Agentic mode optimises batched tool dispatch; Ask mode optimises safe explanation; Code mode optimises copy-paste implementation guidance.

\begin{table}[!ht]
\centering
\caption{Cross-mode summary of reported benchmarks.}
\label{tab:cross-mode}
\small
\begin{tabular}{|l|c|c|p{4.2cm}|}
\hline
\textbf{Mode} & \textbf{Tests reported} & \textbf{Side effects} & \textbf{Primary strength} \\ \hline
Agentic & 1 (pipeline) & Tool dispatch only & 37-tool batch after query call \\
Ask & 3 & None & Cell explanation and error diagnosis \\
Code & 4 & None & Runnable cells + empty-cell deferral \\ \hline
\end{tabular}
\end{table}

In aggregate, the benchmarks show that mode-specific prompting and tool gating produce distinguishable behaviour profiles on the same notebook corpus---supporting the architectural separation advocated in Chapters~\ref{c-intro} and~\ref{c-review}.

\section{Evaluation Metrics and Scoring Rubrics}
\label{sec:results-metrics}

This section defines the automated metrics underlying Tables~\ref{tab:ask-results}--\ref{tab:cross-mode}. Full definitions appear in Appendix~\ref{app:metrics}.

\subsection{Ask mode scoring}

\textbf{Topic coverage} counts rubric keywords present in the streamed reply. Test~1 used nine workflow topics (loading, cleaning, missing values, features, training, model, evaluation, visualisation). The reported 89\% figure indicates that eight of nine topic families were addressed with notebook-grounded language.

\textbf{Hallucination heuristic} flags statements about cell content that cannot be verified from the packed snapshot. Test~2 achieved 0\% because all cited variables (\texttt{date\_added}, drop operations) appear in cell~17 source text.

\textbf{Contract checks} assert zero \texttt{tool\_calls} in completion metadata and zero mock browser dispatches in harness logs.

\subsection{Code mode scoring}

\textbf{Code correctness} applies keyword alignment between the fenced \texttt{python} block and expected terms defined in the benchmark JSON (e.g., \texttt{XGBRegressor}, \texttt{fit}, \texttt{X\_train}). Test~1 achieved 100\% alignment.

\textbf{Placement accuracy} compares cited insert indices against an expected band. When the model cites early notebook cells for a training task, placement may score low even when code is correct---a documented limitation in Section~\ref{sec:code-limitations}.

\subsection{Agentic dispatch scoring}

Agentic success is binary at the harness level: did round~1 emit the expected tool \emph{types} for the scenario? Test~1 passes because insert, edit, and run tools were dispatched in batch after the query round. Tests~2--4 are not reported as primary evidence because they require verification semantics the current artefact does not implement.

\section{Agentic Mode: Detailed Analysis}
\label{sec:agentic-detail}

\subsection{Test 1 --- Complete ML Pipeline}

The user prompt requested a full machine-learning workflow: exploratory analysis, missing-value handling, categorical encoding, multi-model training, comparison, visualisation, and evaluation (Appendix~\ref{app:agent-prompt}). Under the two-call contract, the model could not iteratively observe kernel output; instead it planned the entire workflow as a parallel implement batch.

Table~\ref{tab:agentic-rounds} decomposes the turn into query and implement phases.

\begin{table}[!ht]
\centering
\caption{Agentic Test~1: per-round behaviour.}
\label{tab:agentic-rounds}
\small
\begin{tabular}{|c|p{3.2cm}|r|p{5.8cm}|}
\hline
\textbf{Round} & \textbf{Phase} & \textbf{Tools} & \textbf{Behaviour} \\ \hline
0 & Query & 2 & \texttt{list\_cells}, \texttt{get\_cell} for structure inspection \\ \hline
1 & Implement & 35 & Parallel insert, edit, run batch for pipeline cells \\ \hline
\end{tabular}
\end{table}

The implement batch pattern---equal counts of insert, edit, and run operations---suggests the model paired each new cell with source text and an execution request. This aligns with tool-learning findings that models can compose multi-step plans when schemas are explicit \parencite{schick2023toolformer,qin2024toolllm}. The 9.8\,s wall time reflects Cerebras inference latency plus local tool execution against snapshots, not live Kaggle kernel time.

\subsection{Tests not reported as primary evidence}

The harness includes four Agentic scenarios. Tests~2--4 (error repair, massive batch, target-cell dispatch) evaluate behaviours that depend on verification and replanning. Because the present host omits a ReAct loop, those tests are documented for completeness but excluded from thesis claims, consistent with Section~\ref{sec:evaluation}.

\section{Ask Mode: Per-Test Narratives}
\label{sec:ask-detail}

\subsection{Test 1 --- Explain notebook workflow}

The model produced a structured narrative covering data ingestion, cleaning, feature construction, model fitting, and evaluation metrics. Topic coverage reached 89\%, indicating strong \emph{context awareness} of standard data-science stages even when the packed window did not include every cell in the 80-cell notebook \parencite{lewis2020rag}. The assistant referenced notebook-specific variables where visible and avoided generating executable code, satisfying the Ask contract.

When context packing truncated later cells, the reply appropriately remained high-level rather than inventing cell-level detail---desirable anti-hallucination behaviour for workflow questions \parencite{vaithilingam2022expectation}.

\subsection{Test 2 --- Explain cell 17}

Cell~17 performs date-related preprocessing on the housing dataset. The model identified inputs (\texttt{date\_added}), outputs (mutated dataframe), dependencies on prior loading cells, and the cell's role in the cleaning pipeline. The hallucination heuristic recorded 0\%: no ungrounded claims about variables or outputs absent from the snapshot.

\subsection{Test 3 --- Debug error in cell 31}

On the testing-ol notebook, cell~31 raises a \texttt{NameError} because \texttt{df} is undefined. The model correctly attributed the failure to a missing or out-of-scope dataframe reference, proposed a minimal fix (define or import \texttt{df} from an upstream cell), and described downstream impact if left unresolved. No replacement script was emitted, matching the prompt constraint.

\section{Code Mode: Per-Test Narratives}
\label{sec:code-detail}

\subsection{Test 1 --- XGBoost pipeline generation}

The reply included a runnable \texttt{python} cell using \texttt{XGBRegressor} with \texttt{X\_train} and \texttt{y\_train} drawn from notebook context. Code correctness scored 100\%. Placement guidance cited cells in the modelling section; users should verify indices before inserting, as discussed in Section~\ref{sec:code-limitations}.

\subsection{Test 2 --- Cell 21 replacement}

The model emitted a replacement preprocessing cell preserving \texttt{log\_price} and \texttt{df1} conventions established earlier in the notebook. Placement explicitly targeted cell~21. Keyword alignment with ground-truth terms confirmed compatibility with downstream feature cells.

\subsection{Test 3 --- Feature-engineering module}

The generated cell combined interaction terms, \texttt{PolynomialFeatures}, and missing-value imputation (\texttt{SimpleImputer} or \texttt{fillna}). Placement and run-order bullets preceded the code block, supporting the copy-paste workflow Code mode is designed for \parencite{mcnutt2023notebookassistants}.

\subsection{Test 4 --- Empty cell workflow}
\label{sec:code-empty}

Cell~50 was cleared in the fixture snapshot. The model acknowledged emptiness, asked what the user intended to implement, and deferred code generation---matching the \texttt{CODE\_MODE.txt} deferral pattern. This behaviour protects student agency in educational settings \parencite{finnieansley2022codex}.

\section{Code Mode Limitation}
\label{sec:code-limitations}

Placement guidance can misalign with optimal execution flow when the packed context emphasises early cells. For example, a training cell may be suggested after cell~5 when the notebook's model section begins near cell~40. The generated code remains syntactically valid and uses correct variable names; only positional advice suffers. This mirrors IDE copilot studies where suggestion quality tracks visible context depth \parencite{ziegler2024copilot,mozannar2024reading}.

\section{Timing and Resource Profile}
\label{sec:timing}

Table~\ref{tab:timing} summarises approximate response characteristics across modes. Ask mode averaged the longest wall times because workflow explanations produce lengthy prose without tool amortisation. Agentic Test~1 completed in under ten seconds despite 37 tool dispatches because round~1 emitted a single parallel batch.

\begin{table}[!ht]
\centering
\caption{Approximate timing and resource use (live LLM runs).}
\label{tab:timing}
\small
\begin{tabular}{|l|r|r|r|}
\hline
\textbf{Mode} & \textbf{LLM calls (avg)} & \textbf{Tools (avg)} & \textbf{Time (s, approx)} \\ \hline
Ask & 1 & 0 & 60--80 \\
Code & 1 & 0 & 55--70 \\
Agentic (Test~1) & 2 & 37 & 9.8 \\ \hline
\end{tabular}
\end{table}

The two-call Agentic cap bounds cost per user message: practitioners can predict at most two billed completions regardless of batch size \parencite{qin2024toolllm}.

\section{Qualitative Observations}

Three qualitative patterns emerged across suites:

\begin{enumerate}
  \item \textbf{Mode isolation.} No Ask run invoked tools; no Code run dispatched browser writes; only Agentic round~1 mutated notebook state (via mock dispatch).
  \item \textbf{Grounding discipline.} Cell-local Ask tasks cited snapshot evidence; workflow tasks summarised stages without fabricating invisible cells.
  \item \textbf{Batch planning.} Agentic Test~1 demonstrated that GLM~4.7 can emit dozens of parallel \texttt{tool\_calls} when the implement phase is unconstrained by mid-batch feedback.
\end{enumerate}

\section{Comparative Results Matrix}
\label{sec:results-matrix}

Table~\ref{tab:full-matrix} consolidates reported benchmark outcomes. Cells marked ``---'' indicate metrics not applied to that mode per the evaluation contract in Section~\ref{sec:evaluation}.

\begin{table}[!ht]
\centering
\caption{Consolidated benchmark results matrix (reported tests only).}
\label{tab:full-matrix}
\footnotesize
\begin{tabular}{|l|c|c|c|c|}
\hline
\textbf{Metric} & \textbf{Ask T1} & \textbf{Ask T2--3} & \textbf{Code (all)} & \textbf{Agentic T1} \\ \hline
Pass / strong & Strong & Pass & Pass (4/4) & Pass \\ \hline
Tool calls & 0 & 0 & 0 & 37 \\ \hline
LLM calls & 1 & 1 & 1 & 2 \\ \hline
Browser writes & 0 & 0 & 0 & dispatched \\ \hline
Topic coverage & 89\% & --- & --- & --- \\ \hline
Hallucination (heur.) & --- & 0\% (T2) & --- & --- \\ \hline
Code correctness & --- & --- & 100\% (T1) & --- \\ \hline
Time (s, approx.) & 60--80 & 60--80 & 55--70 & 9.8 \\ \hline
\end{tabular}
\end{table}

\section{Implications for System Design}
\label{sec:results-design}

The matrix supports three design conclusions:

\begin{enumerate}
  \item \textbf{Side-effect control is enforceable.} Ask and Code suites recorded zero browser mutations across all reported tests, validating host-side tool filtering independent of model behaviour.
  \item \textbf{Cell-local tasks outperform workflow tasks under fixed context windows.} Ask Tests~2--3 succeeded with precise evidence; Test~1 succeeded on topic coverage but not exhaustive cell tracing---informing future retrieval investment.
  \item \textbf{Batch dispatch scales within a single implement round.} Agentic Test~1 shows that parallel \texttt{tool\_calls} can express an entire pipeline when query round context suffices for planning.
\end{enumerate}

These conclusions inform the discussion in Chapter~\ref{chap:discussion} and the roadmap in Section~\ref{sec:roadmap}.
